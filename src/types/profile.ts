export type Profile = {
    id: string,
    fullname: string,
    email: string,
    contact: string,
    overview: string,
    photo: string,
    background: string
    liveIn: string,
    available: string,
    degree: string,
    birthOfDay: string
}